Para descargar(actualizar) el codigo existente en el PC
1 >> git pull origin main

Para Subir el codigo a la nube
1 >> git add .
2 >> git commit -m "<Cambios realizados>"
3 >> git push origin main

Para sincronizar nube con local:
1 >> git pull origin main --rebase